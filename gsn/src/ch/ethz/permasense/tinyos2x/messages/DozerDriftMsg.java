/**
 * This class is automatically generated by mig. DO NOT EDIT THIS FILE.
 * This class implements a Java interface to the 'DozerDriftMsg'
 * message type.
 */

package ch.ethz.permasense.tinyos2x.messages;

public class DozerDriftMsg extends ch.ethz.permasense.tinyos2x.messages.DataHeaderMsg {

    /** The default size of this message type in bytes. */
    public static final int DEFAULT_MESSAGE_SIZE = 25;

    /** The Active Message type associated with this message. */
    public static final int AM_TYPE = 150;

    /** Create a new DozerDriftMsg of size 25. */
    public DozerDriftMsg() {
        super(DEFAULT_MESSAGE_SIZE);
        amTypeSet(AM_TYPE);
    }

    /** Create a new DozerDriftMsg of the given data_length. */
    public DozerDriftMsg(int data_length) {
        super(data_length);
        amTypeSet(AM_TYPE);
    }

    /**
     * Create a new DozerDriftMsg with the given data_length
     * and base offset.
     */
    public DozerDriftMsg(int data_length, int base_offset) {
        super(data_length, base_offset);
        amTypeSet(AM_TYPE);
    }

    /**
     * Create a new DozerDriftMsg using the given byte array
     * as backing store.
     */
    public DozerDriftMsg(byte[] data) {
        super(data);
        amTypeSet(AM_TYPE);
    }

    /**
     * Create a new DozerDriftMsg using the given byte array
     * as backing store, with the given base offset.
     */
    public DozerDriftMsg(byte[] data, int base_offset) {
        super(data, base_offset);
        amTypeSet(AM_TYPE);
    }

    /**
     * Create a new DozerDriftMsg using the given byte array
     * as backing store, with the given base offset and data length.
     */
    public DozerDriftMsg(byte[] data, int base_offset, int data_length) {
        super(data, base_offset, data_length);
        amTypeSet(AM_TYPE);
    }

    /**
     * Create a new DozerDriftMsg embedded in the given message
     * at the given base offset.
     */
    public DozerDriftMsg(net.tinyos.message.Message msg, int base_offset) {
        super(msg, base_offset, DEFAULT_MESSAGE_SIZE);
        amTypeSet(AM_TYPE);
    }

    /**
     * Create a new DozerDriftMsg embedded in the given message
     * at the given base offset and length.
     */
    public DozerDriftMsg(net.tinyos.message.Message msg, int base_offset, int data_length) {
        super(msg, base_offset, data_length);
        amTypeSet(AM_TYPE);
    }

    /**
    /* Return a String representation of this message. Includes the
     * message type name and the non-indexed field values.
     */
    public String toString() {
      String s = "Message <DozerDriftMsg> \n";
      try {
        s += "  [header.seqNr=0x"+Long.toHexString(get_header_seqNr())+"]\n";
      } catch (ArrayIndexOutOfBoundsException aioobe) { /* Skip field */ }
      try {
        s += "  [header.originatorID=0x"+Long.toHexString(get_header_originatorID())+"]\n";
      } catch (ArrayIndexOutOfBoundsException aioobe) { /* Skip field */ }
      try {
        s += "  [header.aTime.low=0x"+Long.toHexString(get_header_aTime_low())+"]\n";
      } catch (ArrayIndexOutOfBoundsException aioobe) { /* Skip field */ }
      try {
        s += "  [header.aTime.high=0x"+Long.toHexString(get_header_aTime_high())+"]\n";
      } catch (ArrayIndexOutOfBoundsException aioobe) { /* Skip field */ }
      try {
        s += "  [payload.myTemp=";
        for (int i = 0; i < 3; i++) {
          s += "0x"+Long.toHexString(getElement_payload_myTemp(i) & 0xffff)+" ";
        }
        s += "]\n";
      } catch (ArrayIndexOutOfBoundsException aioobe) { /* Skip field */ }
      try {
        s += "  [payload.parentTemp=";
        for (int i = 0; i < 3; i++) {
          s += "0x"+Long.toHexString(getElement_payload_parentTemp(i) & 0xffff)+" ";
        }
        s += "]\n";
      } catch (ArrayIndexOutOfBoundsException aioobe) { /* Skip field */ }
      try {
        s += "  [payload.driftPPM=";
        for (int i = 0; i < 3; i++) {
          s += "0x"+Long.toHexString(getElement_payload_driftPPM(i) & 0xffff)+" ";
        }
        s += "]\n";
      } catch (ArrayIndexOutOfBoundsException aioobe) { /* Skip field */ }
      return s;
    }

    // Message-type-specific access methods appear below.

    /////////////////////////////////////////////////////////
    // Accessor methods for field: header.seqNr
    //   Field type: int
    //   Offset (bits): 0
    //   Size (bits): 16
    /////////////////////////////////////////////////////////

    /**
     * Return whether the field 'header.seqNr' is signed (false).
     */
    public static boolean isSigned_header_seqNr() {
        return false;
    }

    /**
     * Return whether the field 'header.seqNr' is an array (false).
     */
    public static boolean isArray_header_seqNr() {
        return false;
    }

    /**
     * Return the offset (in bytes) of the field 'header.seqNr'
     */
    public static int offset_header_seqNr() {
        return (0 / 8);
    }

    /**
     * Return the offset (in bits) of the field 'header.seqNr'
     */
    public static int offsetBits_header_seqNr() {
        return 0;
    }

    /**
     * Return the value (as a int) of the field 'header.seqNr'
     */
    public int get_header_seqNr() {
        return (int)getUIntBEElement(offsetBits_header_seqNr(), 16);
    }

    /**
     * Set the value of the field 'header.seqNr'
     */
    public void set_header_seqNr(int value) {
        setUIntBEElement(offsetBits_header_seqNr(), 16, value);
    }

    /**
     * Return the size, in bytes, of the field 'header.seqNr'
     */
    public static int size_header_seqNr() {
        return (16 / 8);
    }

    /**
     * Return the size, in bits, of the field 'header.seqNr'
     */
    public static int sizeBits_header_seqNr() {
        return 16;
    }

    /////////////////////////////////////////////////////////
    // Accessor methods for field: header.originatorID
    //   Field type: int
    //   Offset (bits): 16
    //   Size (bits): 16
    /////////////////////////////////////////////////////////

    /**
     * Return whether the field 'header.originatorID' is signed (false).
     */
    public static boolean isSigned_header_originatorID() {
        return false;
    }

    /**
     * Return whether the field 'header.originatorID' is an array (false).
     */
    public static boolean isArray_header_originatorID() {
        return false;
    }

    /**
     * Return the offset (in bytes) of the field 'header.originatorID'
     */
    public static int offset_header_originatorID() {
        return (16 / 8);
    }

    /**
     * Return the offset (in bits) of the field 'header.originatorID'
     */
    public static int offsetBits_header_originatorID() {
        return 16;
    }

    /**
     * Return the value (as a int) of the field 'header.originatorID'
     */
    public int get_header_originatorID() {
        return (int)getUIntBEElement(offsetBits_header_originatorID(), 16);
    }

    /**
     * Set the value of the field 'header.originatorID'
     */
    public void set_header_originatorID(int value) {
        setUIntBEElement(offsetBits_header_originatorID(), 16, value);
    }

    /**
     * Return the size, in bytes, of the field 'header.originatorID'
     */
    public static int size_header_originatorID() {
        return (16 / 8);
    }

    /**
     * Return the size, in bits, of the field 'header.originatorID'
     */
    public static int sizeBits_header_originatorID() {
        return 16;
    }

    /////////////////////////////////////////////////////////
    // Accessor methods for field: header.aTime.low
    //   Field type: int
    //   Offset (bits): 32
    //   Size (bits): 16
    /////////////////////////////////////////////////////////

    /**
     * Return whether the field 'header.aTime.low' is signed (false).
     */
    public static boolean isSigned_header_aTime_low() {
        return false;
    }

    /**
     * Return whether the field 'header.aTime.low' is an array (false).
     */
    public static boolean isArray_header_aTime_low() {
        return false;
    }

    /**
     * Return the offset (in bytes) of the field 'header.aTime.low'
     */
    public static int offset_header_aTime_low() {
        return (32 / 8);
    }

    /**
     * Return the offset (in bits) of the field 'header.aTime.low'
     */
    public static int offsetBits_header_aTime_low() {
        return 32;
    }

    /**
     * Return the value (as a int) of the field 'header.aTime.low'
     */
    public int get_header_aTime_low() {
        return (int)getUIntBEElement(offsetBits_header_aTime_low(), 16);
    }

    /**
     * Set the value of the field 'header.aTime.low'
     */
    public void set_header_aTime_low(int value) {
        setUIntBEElement(offsetBits_header_aTime_low(), 16, value);
    }

    /**
     * Return the size, in bytes, of the field 'header.aTime.low'
     */
    public static int size_header_aTime_low() {
        return (16 / 8);
    }

    /**
     * Return the size, in bits, of the field 'header.aTime.low'
     */
    public static int sizeBits_header_aTime_low() {
        return 16;
    }

    /////////////////////////////////////////////////////////
    // Accessor methods for field: header.aTime.high
    //   Field type: short
    //   Offset (bits): 48
    //   Size (bits): 8
    /////////////////////////////////////////////////////////

    /**
     * Return whether the field 'header.aTime.high' is signed (false).
     */
    public static boolean isSigned_header_aTime_high() {
        return false;
    }

    /**
     * Return whether the field 'header.aTime.high' is an array (false).
     */
    public static boolean isArray_header_aTime_high() {
        return false;
    }

    /**
     * Return the offset (in bytes) of the field 'header.aTime.high'
     */
    public static int offset_header_aTime_high() {
        return (48 / 8);
    }

    /**
     * Return the offset (in bits) of the field 'header.aTime.high'
     */
    public static int offsetBits_header_aTime_high() {
        return 48;
    }

    /**
     * Return the value (as a short) of the field 'header.aTime.high'
     */
    public short get_header_aTime_high() {
        return (short)getUIntBEElement(offsetBits_header_aTime_high(), 8);
    }

    /**
     * Set the value of the field 'header.aTime.high'
     */
    public void set_header_aTime_high(short value) {
        setUIntBEElement(offsetBits_header_aTime_high(), 8, value);
    }

    /**
     * Return the size, in bytes, of the field 'header.aTime.high'
     */
    public static int size_header_aTime_high() {
        return (8 / 8);
    }

    /**
     * Return the size, in bits, of the field 'header.aTime.high'
     */
    public static int sizeBits_header_aTime_high() {
        return 8;
    }

    /////////////////////////////////////////////////////////
    // Accessor methods for field: payload.myTemp
    //   Field type: int[]
    //   Offset (bits): 56
    //   Size of each element (bits): 16
    /////////////////////////////////////////////////////////

    /**
     * Return whether the field 'payload.myTemp' is signed (false).
     */
    public static boolean isSigned_payload_myTemp() {
        return false;
    }

    /**
     * Return whether the field 'payload.myTemp' is an array (true).
     */
    public static boolean isArray_payload_myTemp() {
        return true;
    }

    /**
     * Return the offset (in bytes) of the field 'payload.myTemp'
     */
    public static int offset_payload_myTemp(int index1) {
        int offset = 56;
        if (index1 < 0 || index1 >= 3) throw new ArrayIndexOutOfBoundsException();
        offset += 0 + index1 * 16;
        return (offset / 8);
    }

    /**
     * Return the offset (in bits) of the field 'payload.myTemp'
     */
    public static int offsetBits_payload_myTemp(int index1) {
        int offset = 56;
        if (index1 < 0 || index1 >= 3) throw new ArrayIndexOutOfBoundsException();
        offset += 0 + index1 * 16;
        return offset;
    }

    /**
     * Return the entire array 'payload.myTemp' as a int[]
     */
    public int[] get_payload_myTemp() {
        int[] tmp = new int[3];
        for (int index0 = 0; index0 < numElements_payload_myTemp(0); index0++) {
            tmp[index0] = getElement_payload_myTemp(index0);
        }
        return tmp;
    }

    /**
     * Set the contents of the array 'payload.myTemp' from the given int[]
     */
    public void set_payload_myTemp(int[] value) {
        for (int index0 = 0; index0 < value.length; index0++) {
            setElement_payload_myTemp(index0, value[index0]);
        }
    }

    /**
     * Return an element (as a int) of the array 'payload.myTemp'
     */
    public int getElement_payload_myTemp(int index1) {
        return (int)getUIntBEElement(offsetBits_payload_myTemp(index1), 16);
    }

    /**
     * Set an element of the array 'payload.myTemp'
     */
    public void setElement_payload_myTemp(int index1, int value) {
        setUIntBEElement(offsetBits_payload_myTemp(index1), 16, value);
    }

    /**
     * Return the total size, in bytes, of the array 'payload.myTemp'
     */
    public static int totalSize_payload_myTemp() {
        return (48 / 8);
    }

    /**
     * Return the total size, in bits, of the array 'payload.myTemp'
     */
    public static int totalSizeBits_payload_myTemp() {
        return 48;
    }

    /**
     * Return the size, in bytes, of each element of the array 'payload.myTemp'
     */
    public static int elementSize_payload_myTemp() {
        return (16 / 8);
    }

    /**
     * Return the size, in bits, of each element of the array 'payload.myTemp'
     */
    public static int elementSizeBits_payload_myTemp() {
        return 16;
    }

    /**
     * Return the number of dimensions in the array 'payload.myTemp'
     */
    public static int numDimensions_payload_myTemp() {
        return 1;
    }

    /**
     * Return the number of elements in the array 'payload.myTemp'
     */
    public static int numElements_payload_myTemp() {
        return 3;
    }

    /**
     * Return the number of elements in the array 'payload.myTemp'
     * for the given dimension.
     */
    public static int numElements_payload_myTemp(int dimension) {
      int array_dims[] = { 3,  };
        if (dimension < 0 || dimension >= 1) throw new ArrayIndexOutOfBoundsException();
        if (array_dims[dimension] == 0) throw new IllegalArgumentException("Array dimension "+dimension+" has unknown size");
        return array_dims[dimension];
    }

    /////////////////////////////////////////////////////////
    // Accessor methods for field: payload.parentTemp
    //   Field type: int[]
    //   Offset (bits): 104
    //   Size of each element (bits): 16
    /////////////////////////////////////////////////////////

    /**
     * Return whether the field 'payload.parentTemp' is signed (false).
     */
    public static boolean isSigned_payload_parentTemp() {
        return false;
    }

    /**
     * Return whether the field 'payload.parentTemp' is an array (true).
     */
    public static boolean isArray_payload_parentTemp() {
        return true;
    }

    /**
     * Return the offset (in bytes) of the field 'payload.parentTemp'
     */
    public static int offset_payload_parentTemp(int index1) {
        int offset = 104;
        if (index1 < 0 || index1 >= 3) throw new ArrayIndexOutOfBoundsException();
        offset += 0 + index1 * 16;
        return (offset / 8);
    }

    /**
     * Return the offset (in bits) of the field 'payload.parentTemp'
     */
    public static int offsetBits_payload_parentTemp(int index1) {
        int offset = 104;
        if (index1 < 0 || index1 >= 3) throw new ArrayIndexOutOfBoundsException();
        offset += 0 + index1 * 16;
        return offset;
    }

    /**
     * Return the entire array 'payload.parentTemp' as a int[]
     */
    public int[] get_payload_parentTemp() {
        int[] tmp = new int[3];
        for (int index0 = 0; index0 < numElements_payload_parentTemp(0); index0++) {
            tmp[index0] = getElement_payload_parentTemp(index0);
        }
        return tmp;
    }

    /**
     * Set the contents of the array 'payload.parentTemp' from the given int[]
     */
    public void set_payload_parentTemp(int[] value) {
        for (int index0 = 0; index0 < value.length; index0++) {
            setElement_payload_parentTemp(index0, value[index0]);
        }
    }

    /**
     * Return an element (as a int) of the array 'payload.parentTemp'
     */
    public int getElement_payload_parentTemp(int index1) {
        return (int)getUIntBEElement(offsetBits_payload_parentTemp(index1), 16);
    }

    /**
     * Set an element of the array 'payload.parentTemp'
     */
    public void setElement_payload_parentTemp(int index1, int value) {
        setUIntBEElement(offsetBits_payload_parentTemp(index1), 16, value);
    }

    /**
     * Return the total size, in bytes, of the array 'payload.parentTemp'
     */
    public static int totalSize_payload_parentTemp() {
        return (48 / 8);
    }

    /**
     * Return the total size, in bits, of the array 'payload.parentTemp'
     */
    public static int totalSizeBits_payload_parentTemp() {
        return 48;
    }

    /**
     * Return the size, in bytes, of each element of the array 'payload.parentTemp'
     */
    public static int elementSize_payload_parentTemp() {
        return (16 / 8);
    }

    /**
     * Return the size, in bits, of each element of the array 'payload.parentTemp'
     */
    public static int elementSizeBits_payload_parentTemp() {
        return 16;
    }

    /**
     * Return the number of dimensions in the array 'payload.parentTemp'
     */
    public static int numDimensions_payload_parentTemp() {
        return 1;
    }

    /**
     * Return the number of elements in the array 'payload.parentTemp'
     */
    public static int numElements_payload_parentTemp() {
        return 3;
    }

    /**
     * Return the number of elements in the array 'payload.parentTemp'
     * for the given dimension.
     */
    public static int numElements_payload_parentTemp(int dimension) {
      int array_dims[] = { 3,  };
        if (dimension < 0 || dimension >= 1) throw new ArrayIndexOutOfBoundsException();
        if (array_dims[dimension] == 0) throw new IllegalArgumentException("Array dimension "+dimension+" has unknown size");
        return array_dims[dimension];
    }

    /////////////////////////////////////////////////////////
    // Accessor methods for field: payload.driftPPM
    //   Field type: short[]
    //   Offset (bits): 152
    //   Size of each element (bits): 16
    /////////////////////////////////////////////////////////

    /**
     * Return whether the field 'payload.driftPPM' is signed (false).
     */
    public static boolean isSigned_payload_driftPPM() {
        return false;
    }

    /**
     * Return whether the field 'payload.driftPPM' is an array (true).
     */
    public static boolean isArray_payload_driftPPM() {
        return true;
    }

    /**
     * Return the offset (in bytes) of the field 'payload.driftPPM'
     */
    public static int offset_payload_driftPPM(int index1) {
        int offset = 152;
        if (index1 < 0 || index1 >= 3) throw new ArrayIndexOutOfBoundsException();
        offset += 0 + index1 * 16;
        return (offset / 8);
    }

    /**
     * Return the offset (in bits) of the field 'payload.driftPPM'
     */
    public static int offsetBits_payload_driftPPM(int index1) {
        int offset = 152;
        if (index1 < 0 || index1 >= 3) throw new ArrayIndexOutOfBoundsException();
        offset += 0 + index1 * 16;
        return offset;
    }

    /**
     * Return the entire array 'payload.driftPPM' as a short[]
     */
    public short[] get_payload_driftPPM() {
        short[] tmp = new short[3];
        for (int index0 = 0; index0 < numElements_payload_driftPPM(0); index0++) {
            tmp[index0] = getElement_payload_driftPPM(index0);
        }
        return tmp;
    }

    /**
     * Set the contents of the array 'payload.driftPPM' from the given short[]
     */
    public void set_payload_driftPPM(short[] value) {
        for (int index0 = 0; index0 < value.length; index0++) {
            setElement_payload_driftPPM(index0, value[index0]);
        }
    }

    /**
     * Return an element (as a short) of the array 'payload.driftPPM'
     */
    public short getElement_payload_driftPPM(int index1) {
        return (short)getSIntBEElement(offsetBits_payload_driftPPM(index1), 16);
    }

    /**
     * Set an element of the array 'payload.driftPPM'
     */
    public void setElement_payload_driftPPM(int index1, short value) {
        setSIntBEElement(offsetBits_payload_driftPPM(index1), 16, value);
    }

    /**
     * Return the total size, in bytes, of the array 'payload.driftPPM'
     */
    public static int totalSize_payload_driftPPM() {
        return (48 / 8);
    }

    /**
     * Return the total size, in bits, of the array 'payload.driftPPM'
     */
    public static int totalSizeBits_payload_driftPPM() {
        return 48;
    }

    /**
     * Return the size, in bytes, of each element of the array 'payload.driftPPM'
     */
    public static int elementSize_payload_driftPPM() {
        return (16 / 8);
    }

    /**
     * Return the size, in bits, of each element of the array 'payload.driftPPM'
     */
    public static int elementSizeBits_payload_driftPPM() {
        return 16;
    }

    /**
     * Return the number of dimensions in the array 'payload.driftPPM'
     */
    public static int numDimensions_payload_driftPPM() {
        return 1;
    }

    /**
     * Return the number of elements in the array 'payload.driftPPM'
     */
    public static int numElements_payload_driftPPM() {
        return 3;
    }

    /**
     * Return the number of elements in the array 'payload.driftPPM'
     * for the given dimension.
     */
    public static int numElements_payload_driftPPM(int dimension) {
      int array_dims[] = { 3,  };
        if (dimension < 0 || dimension >= 1) throw new ArrayIndexOutOfBoundsException();
        if (array_dims[dimension] == 0) throw new IllegalArgumentException("Array dimension "+dimension+" has unknown size");
        return array_dims[dimension];
    }

}
